# AnvilSaba の Discord 管理用語

Discord サーバーの構成と、継続して内容を維持するメッセージについて使う用語。

## 用語

**管理対象リソース**:
構成管理の対象として明示的に採用した、または構成管理によって作成したリソース。Guild に存在するだけでは管理対象にならない。

**管理外リソース**:
Guild に存在するが、構成管理の対象として採用していないリソース。希望構成に記載がないことだけでは削除対象にならない。

**管理解除**:
Discord 上のリソースを残したまま、構成管理の対象から外すこと。管理コンテンツの削除とは区別する。

**参照専用リソース**:
属性を管理せず、ほかの管理対象から参照するために論理 ID の宣言を残したリソース。Role や親 Category を参照する用途がある。

**管理属性**:
管理対象リソースの属性のうち、希望構成で値・既定値・解除が明示された属性。設定セットから受け取る属性も含む。

**既定値指定**:
属性を、設定のバージョンに対応して定義された既定値へ揃える明示指定。属性の省略とは異なり、管理対象となる。

**管理メッセージ（Managed Message）**:
ガイドラインや案内など、宣言された内容を継続して維持するメッセージ。Discord のピン留め状態とは別の概念。
_避ける表現_: ピン留めメッセージとの同義扱い。

**論理 ID**:
管理する Role、Channel、Forum Tag、Thread、Message を表示名や並び順によらず識別する名前。Discord が割り当てる Snowflake とは区別する。

**管理スレッド**:
設定で名前と本文を維持する、親メッセージに紐づかないスレッド。所属 Channel は持つ。管理メッセージの補足などに利用し、本文のリンクから参照する。

**希望構成（desired state）**:
管理対象について実現したいと宣言した構成。

**実構成（actual state）**:
対象 Guild に実際に存在する構成。

**対応状態（state）**:
対象 Guild における論理 ID と Discord 上のリソースとの対応。希望構成や実構成そのものとは区別する。

**管理メッセージ群**:
一つの Channel で投稿順と本文をまとめて維持する管理メッセージの集まり。各メッセージは群内の論理 ID で識別する。

**設定セット**:
Role または Channel の共通する管理属性に名前を付け、複数のリソースで再利用する定義。

**解除指定**:
属性の値や、その場所に固有の権限設定を明示的に取り除く指定。管理解除と異なり、希望構成への変更対象となる。
