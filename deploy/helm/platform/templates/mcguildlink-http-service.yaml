apiVersion: v1
kind: Service
metadata:
  name: mcguildlink-http
spec:
  type: ClusterIP
  selector: { app.kubernetes.io/name: mcguildlink }
  ports:
    - name: http
      port: {{ .Values.mcguildlink.http.port }}
      targetPort: http